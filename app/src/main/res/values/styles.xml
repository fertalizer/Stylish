<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!--<style name="AppTheme.ToolBarStyle">-->
        <!--<item name="colorPrimary">#ffffffff</item>-->
    <!--</style>-->

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/corner_radius</item>
        <item name="behavior_peekHeight">532dp</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    
    
    <style name="CatalogTabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="tabIndicatorColor">#3f3a3a</item>
        <item name="tabIndicatorHeight">1dp</item>
        <item name="tabTextAppearance">@style/CatalogTabText</item>
        <item name="tabBackground">@android:color/transparent</item>
    </style>

    <style name="CatalogTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
    </style>

</resources>
